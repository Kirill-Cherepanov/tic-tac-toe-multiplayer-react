import React from 'react';

type Props = { setGameMode: React.Dispatch<React.SetStateAction<string>> };

export default function MultiplayerMenu({ setGameMode }: Props) {
  return <div>MultiplayerMenu</div>;
}
